name: Process
description: |
  Design or optimize workflows, processes, and operations.
  Use when: workflow automation, process improvement, operational efficiency,
  team workflows, approval processes, handoff optimization.

discovery:
  inline: true

outputs:
  discovery_file: "02-discovery.md"
  versioned_resources: true

mental_models:
  dynamic: true
  load_command: "nexus-mental-models --format brief"
  max_rediscovery_rounds: 1

features:
  ears_requirements: false
  incose_quality: false
  correctness_properties: false
