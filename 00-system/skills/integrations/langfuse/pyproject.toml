[project]
name = "langfuse-integration"
version = "0.1.0"
description = "Langfuse integration skills for Nexus"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "langfuse>=2.0.0",
]

[tool.uv]
dev-dependencies = []
