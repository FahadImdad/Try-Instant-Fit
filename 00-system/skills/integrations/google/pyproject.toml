[project]
name = "google"
version = "0.1.0"
description = "Google Workspace integration skills for Nexus"
requires-python = ">=3.10"
dependencies = [
    "google-auth>=2.0.0",
    "google-api-python-client>=2.0.0",
]

[tool.uv]
dev-dependencies = []
